<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>

<BorderPane xmlns="http://javafx.com/javafx/8" xmlns:fx="http://javafx.com/fxml"
            prefWidth="900" prefHeight="500">

    <top>
        <VBox spacing="5">
            <!-- Título -->
            <Label text="HISTORIAL DE PAGOS"
                   style="-fx-font-size: 18px; -fx-font-weight: bold;"/>

            <!-- Panel de filtros -->
            <VBox spacing="10" style="-fx-background-color: #f2f2f2; -fx-border-color: lightgray;">
                <padding>
                    <Insets top="10" right="10" bottom="10" left="10"/>
                </padding>

                <Label text="Filtros de búsqueda" style="-fx-font-weight: bold;"/>

                <HBox spacing="10" alignment="CENTER_LEFT">
                    <Label text="Código de Contrato:"/>
                    <TextField prefWidth="150"/>
                    <Button text="🔍" prefWidth="30"/>
                    <Button text="⬆" prefWidth="30"/>
                    <TextField prefWidth="40"/>
                </HBox>
            </VBox>
        </VBox>
    </top>

    <center>
        <!-- Tabla de historial -->
        <TableView prefHeight="400" style="-fx-border-color: black;">
            <columns>
                <TableColumn text="Número de Periodo" prefWidth="200"/>
                <TableColumn text="Fecha Límite Pago" prefWidth="200"/>
                <TableColumn text="Estado Pago" prefWidth="200"/>
            </columns>
        </TableView>
    </center>

</BorderPane>
